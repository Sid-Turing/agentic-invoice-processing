# Orchestrator model (OpenAI). Vision/extraction runs in the MCP tools server.
OPENAI_API_KEY=sk-...
OPENAI_MODEL_ID=gpt-5.5-2026-04-23

# External MCP tools server (required — the agent's tools live here)
MCP_TOOLS_URL=http://localhost:9001/mcp

# Database (swappable; schema created via `alembic upgrade head`)
DATABASE_URL=postgresql+psycopg2://invoice:invoice@localhost:5434/invoices

# Business config (optional; defaults shown)
TAX_RATE=0.09125
SUPPORTED_CURRENCIES=USD
MAX_UPLOAD_MB=10
